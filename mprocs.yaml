procs:
    backend:
        shell: "sh -c 'pnpm --dir backend db:migrate && exec pnpm --dir backend dev'"
    frontend:
        shell: "pnpm run dev"
    drizzle-studio:
        shell: "pnpm --dir backend db:studio"
        autostart: false
