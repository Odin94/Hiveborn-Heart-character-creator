[build]
command = "pnpm install --frozen-lockfile && pnpm run build"
publish = "dist"

[build.environment]
PNPM_VERSION = "10.33.0"

# Let the React router handle production deep links such as /auth/callback.
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
